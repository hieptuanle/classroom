// import { drizzle } from "drizzle-orm/postgres-js";
import "dotenv/config";

// const DB_URL = process.env.DATABASE_URL as string || "localhost";
// Create drizzle instance
// export const db = drizzle(DB_URL);

// export async function connection() {
//   try {
//     // Test the connection
//     await client`SELECT 1`;
//     logger.info("✅ Successfully connected to PostgreSQL database");

//     // In development, you might want to run migrations here
//     if (getNodeEnv() === "development") {
//       logger.info("✅ Database connection ready for development");
//     }
//   }
//   catch (err) {
//     logger.error("❌ Unable to connect to database: ", err);
//     process.exit(1);
//   }
// }

// // Export the client for direct queries if needed
// export { client };
